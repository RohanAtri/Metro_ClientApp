import { AddAmountPipe } from './add-amount.pipe';

describe('AddAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new AddAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
